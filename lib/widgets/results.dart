import 'package:flutter/material.dart';
/*
import 'package:flutter/material.dart';

class MD extends StatelessWidget {
  MD(
      {Key? key,
      required this.title,
      required this.subtitle,
      required this.trailing,
      this.icon = Icons.label,
      this.titleColor = Colors.brown,
      this.subtitleColor = Colors.brown,
      this.trailingColor = Colors.brown,
      this.tileColor = Colors.white})
      : super(key: key);
  late String title, subtitle, trailing;
  final IconData icon;
  final Color? tileColor, titleColor, subtitleColor, trailingColor;

  @override
  Widget build(BuildContext context) {
    return ListTile(
      leading: const Icon(
        Icons.label,
        color: Colors.brown,
      ),
      title: Column(
        children: [
          Text(title, style: TextStyle(color: titleColor)),
        ],
      ),
      subtitle: Text(subtitle, style: TextStyle(color: subtitleColor)),
      trailing: Text(trailing, style: TextStyle(color: trailingColor)),
      //onTap: () {},
      tileColor: tileColor,
    );
  }
}
*/
